# Generated by the figure scripts in paper_analysis/
figs/
paper_analysis/*.pdf
paper_analysis/*.html

# The organisers' data, placed here by the user (see README)
data/train_task_*.jsonl
data/dev_in.jsonl
data/dev_task_*.jsonl
data/test_in.jsonl
models/

# Our own development predictions carry labels and offsets only, no source text,
# so they are released; anything else under preds/ is not.
preds/*
!preds/task1_dev_*.jsonl
!preds/task2_dev_*.jsonl

__pycache__/
*.pyc
